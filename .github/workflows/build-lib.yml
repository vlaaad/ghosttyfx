name: Build libghostty-vt

on:
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ghostty-org/ghostty
          path: ghostty

      - name: Install Zig
        run: |
          curl -L https://ziglang.org/download/0.15.2/zig-x86_64-linux-0.15.2.tar.xz | tar xJ
          echo "${{ github.workspace }}/zig-x86_64-linux-0.15.2" >> $GITHUB_PATH

      - name: Build
        run: |
          cd ghostty
          zig build -Dapp-runtime=none -Demit-lib-vt=true

      - name: Package
        run: |
          cd ghostty/zig-out
          mkdir -p lib/linux-x64
          test -f lib/libghostty-vt.so.0.1.0
          cp lib/libghostty-vt.so.0.1.0 lib/linux-x64/
          cp lib/libghostty-vt.a lib/linux-x64/ 2>/dev/null || true
          cp -r include lib/linux-x64/
          cp lib/pkgconfig/*.pc lib/linux-x64/ 2>/dev/null || true

      - uses: actions/upload-artifact@v4
        with:
          name: libghostty-vt-linux-x64
          path: ghostty/zig-out/lib/linux-x64/
          retention-days: 1

  build-macos-arm64:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ghostty-org/ghostty
          path: ghostty

      - name: Install Zig
        run: |
          curl -L https://ziglang.org/download/0.15.2/zig-aarch64-macos-0.15.2.tar.xz | tar xJ
          echo "${{ github.workspace }}/zig-aarch64-macos-0.15.2" >> $GITHUB_PATH

      - name: Build
        run: |
          cd ghostty
          zig build -Dapp-runtime=none -Demit-lib-vt=true

      - name: Package
        run: |
          cd ghostty/zig-out
          mkdir -p lib/macos-arm64
          test -f lib/libghostty-vt.0.1.0.dylib
          cp lib/libghostty-vt.0.1.0.dylib lib/macos-arm64/
          cp lib/libghostty-vt*.a lib/macos-arm64/ 2>/dev/null || true
          cp -r include lib/macos-arm64/
          cp lib/pkgconfig/*.pc lib/macos-arm64/ 2>/dev/null || true

      - uses: actions/upload-artifact@v4
        with:
          name: libghostty-vt-macos-arm64
          path: ghostty/zig-out/lib/macos-arm64/
          retention-days: 1

  build-macos-x64:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ghostty-org/ghostty
          path: ghostty

      - name: Install Zig
        run: |
          curl -L https://ziglang.org/download/0.15.2/zig-x86_64-macos-0.15.2.tar.xz | tar xJ
          echo "${{ github.workspace }}/zig-x86_64-macos-0.15.2" >> $GITHUB_PATH

      - name: Build
        run: |
          cd ghostty
          zig build -Dapp-runtime=none -Demit-lib-vt=true

      - name: Package
        run: |
          cd ghostty/zig-out
          mkdir -p lib/macos-x64
          test -f lib/libghostty-vt.0.1.0.dylib
          cp lib/libghostty-vt.0.1.0.dylib lib/macos-x64/
          cp lib/libghostty-vt*.a lib/macos-x64/ 2>/dev/null || true
          cp -r include lib/macos-x64/
          cp lib/pkgconfig/*.pc lib/macos-x64/ 2>/dev/null || true

      - uses: actions/upload-artifact@v4
        with:
          name: libghostty-vt-macos-x64
          path: ghostty/zig-out/lib/macos-x64/
          retention-days: 1

  build-windows:
    runs-on: windows-latest
    env:
      ZIG_LOCAL_CACHE_DIR: ${{ github.workspace }}\.zig-local-cache
      ZIG_GLOBAL_CACHE_DIR: ${{ github.workspace }}\.zig-global-cache
      TEMP: ${{ github.workspace }}\.tmp
      TMP: ${{ github.workspace }}\.tmp
    steps:
      - uses: actions/checkout@v4
        with:
          repository: ghostty-org/ghostty
          path: ghostty

      - name: Setup Zig
        uses: mlugg/setup-zig@d1434d08867e3ee9daa34448df10607b98908d29

      - name: Prepare cache directories
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path $env:ZIG_LOCAL_CACHE_DIR | Out-Null
          New-Item -ItemType Directory -Force -Path $env:ZIG_GLOBAL_CACHE_DIR | Out-Null
          New-Item -ItemType Directory -Force -Path $env:TEMP | Out-Null

      - name: Build
        shell: pwsh
        run: |
          cd ghostty
          zig build -Dapp-runtime=none -Demit-lib-vt=true

      - name: Package
        shell: pwsh
        run: |
          Set-Location ghostty/zig-out
          if (-not (Test-Path bin/ghostty-vt.dll)) {
            throw "Expected Windows shared library at ghostty/zig-out/bin/ghostty-vt.dll"
          }
          New-Item -ItemType Directory -Force -Path lib/windows-x64 | Out-Null
          Copy-Item bin/ghostty-vt.dll lib/windows-x64
          Copy-Item lib/*.lib lib/windows-x64 -ErrorAction SilentlyContinue
          Copy-Item include lib/windows-x64 -Recurse -Force
          Copy-Item lib/pkgconfig/*.pc lib/windows-x64 -ErrorAction SilentlyContinue

      - uses: actions/upload-artifact@v4
        with:
          name: libghostty-vt-windows-x64
          path: ghostty/zig-out/lib/windows-x64/
          retention-days: 1
